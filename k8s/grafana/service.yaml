apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: monitoring
spec:
  type: ClusterIP        # internal only — accessible within the cluster
  selector:
    app: grafana         # ← routes traffic to pods with this label
  ports:
    - port: 3000
      targetPort: 3000
